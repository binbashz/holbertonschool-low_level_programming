#include "main.h"
#include <stdio.h>
#include <stdlib.h>



/* atoi - Converts a string to an integer
 * @s: The pointer to convert
 * Return: converted integer
 */

int _atoi(char *s)
{
	int = n 0, nod = 1;

	while (*s == ' '  (*s => '\t' & *s <= '\r'))
	{
		s++:
	}
       









	return (0)
}
